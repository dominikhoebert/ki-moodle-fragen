# Fragen erzeugen

```
halte dich an `erzeuge-fragen.md`. Erstelle 5 Fragen im Fach `Fach` zur Leitfrage: ""
```

## GIFT-Fragetypen (je eigener Prompt)

```txt
halte dich an `erzeuge-fragen-gift-multiple-choice.md`. Erstelle 5 Fragen im Fach `Fach` zur Leitfrage: ""
```

```txt
halte dich an `erzeuge-fragen-gift-wahr-falsch.md`. Erstelle 5 Fragen im Fach `Fach` zur Leitfrage: ""
```

```txt
halte dich an `erzeuge-fragen-gift-kurzantwort.md`. Erstelle 5 Fragen im Fach `Fach` zur Leitfrage: ""
```

```txt
halte dich an `erzeuge-fragen-gift-zuordnung.md`. Erstelle 5 Fragen im Fach `Fach` zur Leitfrage: ""
```

```txt
halte dich an `erzeuge-fragen-gift-numerisch.md`. Erstelle 5 Fragen im Fach `Fach` zur Leitfrage: ""
```


```
verwende `erzeuge-fragen-sqlite.md`: erzeuge 5 fragen für das fach `Fach`. Beispiele:
```

```sql
verwende `erzeuge-fragen-sqlite.md`: erzeuge 5 fragen für das fach `insy62`. Thema JOIN (INNER, LEFT, RIGHT, NATURAL, CROSS). Folgende Befehle sind erlaubt: SELECT, DISTINCT, WHERE, AND, OR, NOT, BETWEEN, ORDER BY, LIKE, LIMIT, AS, MIN, MAX, AVG, SUM, COUNT. Nicht erlaubt: UNION, INTERSECT, UNION ALL, EXCEPT, GROUP BY, HAVING, SUBSELECTS, SUBQUERIES, IN, EXISTS, ANY, ALL.
```

